# Mobile Design Rules

We use these rules for mobile consistency:

- No horizontal overflow
- Cards stack cleanly
- Buttons remain tappable
- Typography stays readable
- Sticky elements do not block key actions
